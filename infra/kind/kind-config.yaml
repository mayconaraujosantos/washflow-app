kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: washflow-local
nodes:
  - role: control-plane
    extraPortMappings:
      # washflow-api Service (NodePort) -> http://localhost:7000
      - containerPort: 30080
        hostPort: 7000
        protocol: TCP
