kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: washflow-local
nodes:
  - role: control-plane
    extraPortMappings:
      # washflow-api Service (NodePort) -> http://localhost:8080
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
