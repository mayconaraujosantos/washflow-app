apiVersion: v1
kind: Service
metadata:
  name: washflow-api
  labels:
    app: washflow-api
spec:
  type: NodePort
  selector:
    app: washflow-api
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
