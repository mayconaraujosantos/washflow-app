/**
 * Concrete implementations of the ports declared under {@code data.protocols}
 * using real frameworks/libraries (JDBI, H2, BCrypt, JWT, Javalin's Context...).
 * This is the only layer allowed to know about third-party frameworks.
 */
package com.washflow.infra;
