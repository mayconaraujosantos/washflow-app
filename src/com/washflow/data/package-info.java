/**
 * Implements {@code domain.usecases} by orchestrating calls to the
 * protocols (ports) declared in {@link com.washflow.data.protocols}.
 * Depends only on {@code domain} — never on {@code infra}.
 */
package com.washflow.data;
