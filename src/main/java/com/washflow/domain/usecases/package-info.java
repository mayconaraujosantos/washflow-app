/**
 * Use case contracts (interfaces/ports) describing what the application does, e.g. {@code
 * AddAccount}, {@code Authentication}. Implemented by the data layer.
 */
package com.washflow.domain.usecases;
