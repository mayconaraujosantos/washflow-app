/**
 * Concrete use case implementations, e.g. {@code DbAddAccount implements AddAccount}. Each use case
 * typically gets its own sub-package (mirrors clean-ts-api's "usecases/add-account" folder).
 */
package com.washflow.data.usecases;
